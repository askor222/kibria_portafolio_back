{% extends 'base.html.twig' %}

{% block title %}Proyectos index{% endblock %}

{% block body %}
    <h1>Proyectos index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Explication</th>
                <th>Demo</th>
                <th>Github</th>
                <th>Img</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for proyecto in proyectos %}
            <tr>
                <td>{{ proyecto.id }}</td>
                <td>{{ proyecto.name }}</td>
                <td>{{ proyecto.explication }}</td>
                <td>{{ proyecto.demo }}</td>
                <td>{{ proyecto.github }}</td>
                <td>{{ proyecto.img }}</td>
                <td>
                    <a href="{{ path('app_proyectos_show', {'id': proyecto.id}) }}">show</a>
                    <a href="{{ path('app_proyectos_edit', {'id': proyecto.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_proyectos_new') }}">Create new</a>
{% endblock %}
