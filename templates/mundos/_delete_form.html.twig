<form method="post" action="{{ path('app_mundos_delete', {'id': mundo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mundo.id) }}">
    <button class="btn">Delete</button>
</form>
